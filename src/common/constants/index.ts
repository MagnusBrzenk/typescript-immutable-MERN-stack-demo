export { countryCodes } from "./countryCodes";

//The number of contacts to send on each step of infinite scroll
export const feedChunkSize: number = 50;

//Prefix string for the debug calls in `__FUNCTIONS/__debug`
export const debugPrefixString: string = "DEBUG-";

//Id of front-end contacts before created on backend:
export const provisionalNewContactId: string = "new-contact-id";
